name: Hello world
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Say hello
        run: echo "Hello world"
    
      - name: Run build
        run: make run

